# Block 1B-2 / 1D - Correlation Engine Spec

This block prepares the temporal correlation engine that sits between the existing `Isolation Forest` outputs and the future `1C` agentic layer.

Initial scope:
- build temporal windows
- train correlation logic
- score propagation signatures
- export artifacts under explicit contracts
- support smoke validation before any runtime integration
